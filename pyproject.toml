[project]
name = "master-thesis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "laser-learning-environment",
    "python-sat",
    "matplotlib",
    "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]
